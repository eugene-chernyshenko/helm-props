vars:
  backend: "http://backend-service:8080"
  database:
    host: 10.10.10.10

configuration:
  application.yaml:
    server:
      port: 8080

configurationProperties:
  server.properties:
    server:
      port: 8080
